//2
#include "StdAfx.h"
#include "bar.h"
#include <string>
using std::string;

string bar::readTime(string strLine) { // Return the date & time string.
	size_t EndDate, EndTime;
	//EndDate = InStr(strLine, mstrSeprt) 'The end of the date string.
	//return (strchr Left$(strLine, intLeng);
string str = "";
return(str);
}

void bar::readOHLC(string strLine) { // Read the date & time & the open, high, low & close quotes of the bar.
	time = readTime(strLine);
}